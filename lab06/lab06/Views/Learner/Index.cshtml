@model IEnumerable<Learner>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="ms-2 mx-2">
    <h1>Learner List</h1>

    <!-- Hiển thị ViewComponent Major để lọc theo chuyên ngành -->
    @await Component.InvokeAsync("Major")

    <!-- Thêm form tìm kiếm -->
    <form class="d-none d-md-inline-block form-inline ms-auto me-0 me-md-3 my-2 my-md-0">
        <div class="input-group">
            <!-- Input field để nhập từ khóa tìm kiếm -->
            <input id="keyword" class="form-control" name="keyword" type="text" placeholder="Search learner" />
            <!-- Button để thực hiện tìm kiếm -->
            <button id="btnSearch" class="btn btn-primary" type="button">
                🔍 Search
            </button>
        </div>
    </form>

    <!-- Div chứa nội dung bảng học viên, sẽ được cập nhật bằng AJAX -->
    <div id="content">
        <table class="table table-striped">
            <!-- Phần này không có gì thay đổi, giữ nguyên như view Index cũ -->
            <thead>
                <tr>
                    <th>Mã</th>
                    <th>Tên</th>
                    <th>Họ</th>
                    <th>Ngành</th>
                    <th>Ngày Nhập Học</th>
                    <th>Chức năng</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var s in Model)
                {
                    <tr>
                        <td>@s.LearnerID</td>
                        <td>@s.FirstMidName</td>
                        <td>@s.LastName</td>
                        <td>@s.Major?.MajorName</td>
                        <td>@s.EnrollmentDate</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@s.LearnerID">Edit</a> /
                            <a asp-action="Delete" asp-route-id="@s.LearnerID">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Thêm thanh nav-phân trang -->
    <ul class="pagination">
        @for (int i = 1; i <= ViewBag.pageNum; i++)
        {
            <!-- Mỗi page-item đại diện cho một trang -->
            <li class="page-item" page="@i" 
                @(ViewBag.mid != null ? Html.Raw("mid=\"" + ViewBag.mid + "\"") : Html.Raw(""))
                @(ViewBag.keyword != null ? Html.Raw("keyword=\"" + ViewBag.keyword + "\"") : Html.Raw(""))>
                <a class="page-link">@i</a>
            </li>
        }
    </ul>

    <!-- Link để tạo học viên mới -->
    <a asp-action="Create" class="btn btn-primary">Create New Student</a>
</div>

@section Scripts {
    <script>
        // Thêm script AJAX cho click vào page-item, nút search
        $(document).ready(function () {
            // Event listener cho click vào page-item (phân trang)
            $('body').on('click', 'li.page-item', function (e) {
                e.preventDefault();
                
                // Lấy các giá trị từ attributes của page-item
                var page = $(this).attr('page');
                var mid = $(this).attr('mid');
                var keyword = $(this).attr('keyword');
                
                // Chuẩn bị dữ liệu để gửi lên server
                var data = {
                    pageIndex: page
                };
                
                // Nếu có mid thì thêm vào data
                if (mid) {
                    data.mid = mid;
                }
                
                // Nếu có keyword thì thêm vào data
                if (keyword) {
                    data.keyword = keyword;
                }
                
                // Gửi AJAX request đến action LearnerFilter
                $.ajax({
                    url: "LearnerFilter",
                    type: "POST",
                    data: data,
                    success: function (response) {
                        // Cập nhật nội dung div#content với response từ server
                        $('#content').html(response);
                    },
                    error: function () {
                        alert('Có lỗi xảy ra khi tải dữ liệu!');
                    }
                });
            });

            // Event listener cho click vào nav li (lọc theo chuyên ngành)
            $(".nav li").click(function (e) {
                e.preventDefault();
                
                // Lấy ID của chuyên ngành được chọn
                var id = $(this).attr('id');
                
                // Chuẩn bị dữ liệu để gửi lên server
                var data = {
                    mid: id
                };
                
                // Gửi AJAX request đến action LearnerFilter
                $.ajax({
                    url: "LearnerFilter",
                    type: "POST",
                    data: data,
                    success: function (response) {
                        // Cập nhật nội dung div#content với response từ server
                        $('#content').html(response);
                    },
                    error: function () {
                        alert('Có lỗi xảy ra khi tải dữ liệu!');
                    }
                });
            });

            // Event listener cho click vào button search (tìm kiếm)
            $("button#btnSearch").click(function (e) {
                e.preventDefault();
                
                // Lấy giá trị từ input keyword
                var keyword = $("#keyword").val();
                
                // Chuẩn bị dữ liệu để gửi lên server
                var data = {
                    keyword: keyword
                };
                
                // Gửi AJAX request đến action LearnerFilter
                $.ajax({
                    url: "LearnerFilter",
                    type: "POST",
                    data: data,
                    success: function (response) {
                        // Cập nhật nội dung div#content với response từ server
                        $('#content').html(response);
                    },
                    error: function () {
                        alert('Có lỗi xảy ra khi tìm kiếm!');
                    }
                });
            });
        });
    </script>
}

