@{
    ViewData["Title"] = "Trang chủ";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12 mb-4">
            <div class="row text-center">
                <div class="col-md-4">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <h2 class="mb-0">@ViewBag.StudentsCount</h2>
                            <div class="text-muted">Sinh viên</div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mt-3 mt-md-0">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <h2 class="mb-0">@ViewBag.CoursesCount</h2>
                            <div class="text-muted">Khóa học</div>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 mt-3 mt-md-0">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <h2 class="mb-0">@ViewBag.EnrollmentsCount</h2>
                            <div class="text-muted">Lượt đăng ký</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-12">
            <div class="jumbotron bg-primary text-white rounded p-5 mb-4">
                <div class="container-fluid py-5">
                    <h1 class="display-4 fw-bold">
                        <i class="fas fa-graduation-cap"></i> Chào mừng đến với StudentManagement
                    </h1>
                    <p class="lead">
                        Hệ thống quản lý sinh viên được xây dựng bằng ASP.NET Core MVC và Entity Framework Core Code First
                    </p>
                    <hr class="my-4">
                    <p>
                        Ứng dụng này demo việc sử dụng Entity Framework Core theo hướng Code First để quản lý dữ liệu sinh viên, 
                        khóa học và việc đăng ký khóa học.
                    </p>
                    <a class="btn btn-light btn-lg" asp-controller="Students" asp-action="Index" role="button">
                        <i class="fas fa-users"></i> Bắt đầu Quản lý Sinh viên
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-users fa-3x text-primary mb-3"></i>
                    <h5 class="card-title">Quản lý Sinh viên</h5>
                    <p class="card-text">
                        Thêm, sửa, xóa và xem thông tin chi tiết của sinh viên. 
                        Quản lý đầy đủ thông tin cá nhân và học tập.
                    </p>
                    <a asp-controller="Students" asp-action="Index" class="btn btn-primary">
                        <i class="fas fa-arrow-right"></i> Xem chi tiết
                    </a>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-book fa-3x text-success mb-3"></i>
                    <h5 class="card-title">Khóa học</h5>
                    <p class="card-text">
                        Quản lý thông tin các khóa học bao gồm mã khóa học, 
                        tên khóa học, mô tả và số tín chỉ.
                    </p>
                    <a asp-controller="Courses" asp-action="Index" class="btn btn-success">
                        <i class="fas fa-arrow-right"></i> Xem chi tiết
                    </a>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card h-100">
                <div class="card-body text-center">
                    <i class="fas fa-clipboard-list fa-3x text-warning mb-3"></i>
                    <h5 class="card-title">Đăng ký Khóa học</h5>
                    <p class="card-text">
                        Quản lý việc đăng ký khóa học của sinh viên, 
                        theo dõi điểm số và trạng thái học tập.
                    </p>
                    <a asp-controller="Enrollments" asp-action="Index" class="btn btn-warning">
                        <i class="fas fa-arrow-right"></i> Xem chi tiết
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-5">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">
                        <i class="fas fa-chart-pie"></i> Thống kê & Biểu đồ
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <canvas id="chartTopCourses" height="200"></canvas>
                        </div>
                        <div class="col-md-6">
                            <canvas id="chartOverview" height="200"></canvas>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <script>
        (function loadChartJs(){
            var s = document.createElement('script');
            s.src = 'https://cdn.jsdelivr.net/npm/chart.js';
            s.onload = renderCharts;
            document.body.appendChild(s);
        })();

        async function renderCharts(){
            const overviewRes = await fetch('/stats/overview');
            const overview = await overviewRes.json();
            const ctxOverview = document.getElementById('chartOverview').getContext('2d');
            new Chart(ctxOverview, {
                type: 'bar',
                data: {
                    labels: ['Students','Courses','Enrollments','Avg GPA'],
                    datasets: [{
                        label: 'Overview',
                        data: [overview.students, overview.courses, overview.enrollments, overview.avgGpa],
                        backgroundColor: ['#0d6efd','#198754','#ffc107','#6f42c1']
                    }]
                },
                options: { responsive: true, plugins: { legend: { display:false } } }
            });

            const topRes = await fetch('/stats/top-courses');
            const top = await topRes.json();
            const labels = top.map(x => x.title || x.Title);
            const values = top.map(x => x.count || x.Count);
            const ctxTop = document.getElementById('chartTopCourses').getContext('2d');
            new Chart(ctxTop, {
                type: 'pie',
                data: {
                    labels,
                    datasets: [{ data: values, backgroundColor: ['#0dcaf0','#198754','#dc3545','#ffc107','#0d6efd'] }]
                },
                options: { responsive: true }
            });
        }
    </script>
}
